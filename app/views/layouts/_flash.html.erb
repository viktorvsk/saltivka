<% if flash.present? %>
  <div class="mt-6 mb-6">
    <% flash.each do |key, value| %>
      <div class="notification is-<%= key %>">
        <%= value %>
      </div>
    <% end %>
  </div>
<% end %>
